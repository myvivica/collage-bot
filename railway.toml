[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python main.py"
restartPolicyType = "ON_FAILURE"
# polling-бот: новый инстанс не должен пересекаться со старым (Conflict в getUpdates)
overlapSeconds = 0
restartPolicyMaxRetries = 5
